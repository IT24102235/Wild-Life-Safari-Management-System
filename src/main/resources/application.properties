# Intentionally left minimal; prefer application.yml for configuration
spring.application.name=Safari Management System
# Removed JPA ddl-auto to avoid overriding application.yml (validate)
# spring.jpa.hibernate.ddl-auto=validate
# (Other properties migrated to application.yml)
